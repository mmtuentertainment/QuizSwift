---
phase: 01-foundation-compliance
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/lib/auth.config.ts
  - src/lib/auth.ts
  - src/app/api/auth/[...nextauth]/route.ts
  - src/middleware.ts
  - src/app/(auth)/login/page.tsx
  - src/app/(dashboard)/dashboard/page.tsx
  - src/app/(dashboard)/layout.tsx
autonomous: true
user_setup:
  - service: google-oauth
    why: "Google OAuth for teacher authentication"
    env_vars:
      - name: AUTH_GOOGLE_ID
        source: "Google Cloud Console -> APIs & Services -> Credentials -> OAuth 2.0 Client IDs"
      - name: AUTH_GOOGLE_SECRET
        source: "Google Cloud Console -> APIs & Services -> Credentials -> OAuth 2.0 Client IDs"
    dashboard_config:
      - task: "Create OAuth 2.0 Client ID"
        location: "Google Cloud Console -> APIs & Services -> Credentials"
      - task: "Add authorized redirect URI: http://localhost:3000/api/auth/callback/google"
        location: "OAuth 2.0 Client ID settings"
      - task: "Configure OAuth consent screen (External or Internal)"
        location: "Google Cloud Console -> APIs & Services -> OAuth consent screen"

must_haves:
  truths:
    - "Teacher can click 'Sign in with Google' and complete OAuth flow"
    - "After sign-in, teacher is redirected to dashboard"
    - "Session persists across browser refresh (database sessions)"
    - "Unauthenticated users are redirected to login page when accessing /dashboard"
  artifacts:
    - path: "src/lib/auth.ts"
      provides: "Auth.js configuration with Prisma adapter"
      exports: ["handlers", "auth", "signIn", "signOut"]
    - path: "src/lib/auth.config.ts"
      provides: "Edge-compatible auth configuration"
      exports: ["authConfig"]
    - path: "src/app/api/auth/[...nextauth]/route.ts"
      provides: "Auth.js API routes"
      exports: ["GET", "POST"]
    - path: "src/middleware.ts"
      provides: "Route protection middleware"
      contains: "matcher"
  key_links:
    - from: "src/lib/auth.ts"
      to: "src/lib/prisma.ts"
      via: "PrismaAdapter import"
      pattern: "PrismaAdapter\\(prisma\\)"
    - from: "src/app/api/auth/[...nextauth]/route.ts"
      to: "src/lib/auth.ts"
      via: "handlers import"
      pattern: "import.*handlers.*from"
    - from: "src/middleware.ts"
      to: "src/lib/auth.config.ts"
      via: "authConfig import"
      pattern: "import.*authConfig.*from"
---

<objective>
Implement Google OAuth authentication using Auth.js v5 with database sessions for persistent login.

Purpose: Enable teachers to sign in securely using their Google accounts. Database sessions ensure sessions persist across browser refresh (AUTH-04) and can be audited for FERPA compliance.

Output: Working authentication flow - login page with Google sign-in button, protected dashboard route, session persistence, and automatic redirects.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-compliance/01-RESEARCH.md
@.planning/phases/01-foundation-compliance/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure Auth.js v5 with split configuration pattern</name>
  <files>
    - src/lib/auth.config.ts
    - src/lib/auth.ts
    - src/app/api/auth/[...nextauth]/route.ts
    - src/middleware.ts
  </files>
  <action>
Create Auth.js v5 configuration using the split configuration pattern (edge-compatible config separate from database adapter).

Create `src/lib/auth.config.ts` (edge-compatible, no database imports):

```typescript
import type { NextAuthConfig } from "next-auth"
import Google from "next-auth/providers/google"

export const authConfig = {
  providers: [
    Google({
      clientId: process.env.AUTH_GOOGLE_ID!,
      clientSecret: process.env.AUTH_GOOGLE_SECRET!,
    }),
  ],
  pages: {
    signIn: "/login",
  },
  callbacks: {
    authorized({ auth, request: { nextUrl } }) {
      const isLoggedIn = !!auth?.user
      const isOnDashboard = nextUrl.pathname.startsWith("/dashboard")

      if (isOnDashboard) {
        if (isLoggedIn) return true
        return false // Redirect to login
      }

      // Allow access to public pages
      return true
    },
  },
} satisfies NextAuthConfig
```

Create `src/lib/auth.ts` (full config with database adapter):

```typescript
import NextAuth from "next-auth"
import { PrismaAdapter } from "@auth/prisma-adapter"
import prisma from "@/lib/prisma"
import { authConfig } from "./auth.config"

export const { handlers, auth, signIn, signOut } = NextAuth({
  adapter: PrismaAdapter(prisma),
  session: { strategy: "database" }, // Database sessions for persistence + auditability
  ...authConfig,
  callbacks: {
    ...authConfig.callbacks,
    session({ session, user }) {
      // Include user ID in session for audit logging
      if (session.user) {
        session.user.id = user.id
      }
      return session
    },
  },
})
```

Create `src/app/api/auth/[...nextauth]/route.ts`:

```typescript
import { handlers } from "@/lib/auth"

export const { GET, POST } = handlers
```

Create `src/middleware.ts` (uses edge-compatible config only):

```typescript
import NextAuth from "next-auth"
import { authConfig } from "@/lib/auth.config"

export default NextAuth(authConfig).auth

export const config = {
  // Match all paths except static files and API routes (except auth)
  matcher: ["/((?!api|_next/static|_next/image|favicon.ico|.*\\.png$).*)"],
}
```

Add type declaration for session user ID. Create `src/types/next-auth.d.ts`:

```typescript
import { DefaultSession } from "next-auth"

declare module "next-auth" {
  interface Session {
    user: {
      id: string
    } & DefaultSession["user"]
  }
}
```
  </action>
  <verify>
TypeScript compiles without errors: `npx tsc --noEmit`
Auth config exports are correct: verify imports work in route.ts
  </verify>
  <done>
Auth.js v5 configured with Google OAuth provider, database sessions, and edge-compatible middleware. Session includes user ID for audit logging.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create login page and protected dashboard</name>
  <files>
    - src/app/(auth)/login/page.tsx
    - src/app/(dashboard)/dashboard/page.tsx
    - src/app/(dashboard)/layout.tsx
    - src/components/auth/sign-in-button.tsx
    - src/components/auth/sign-out-button.tsx
  </files>
  <action>
Create the login page with Google sign-in button.

Create `src/components/auth/sign-in-button.tsx`:

```tsx
"use client"

import { signIn } from "next-auth/react"

export function SignInButton() {
  return (
    <button
      onClick={() => signIn("google", { callbackUrl: "/dashboard" })}
      className="flex items-center gap-3 rounded-lg border border-gray-300 bg-white px-6 py-3 text-gray-700 shadow-sm transition-colors hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2"
    >
      <svg className="h-5 w-5" viewBox="0 0 24 24">
        <path
          fill="#4285F4"
          d="M22.56 12.25c0-.78-.07-1.53-.2-2.25H12v4.26h5.92c-.26 1.37-1.04 2.53-2.21 3.31v2.77h3.57c2.08-1.92 3.28-4.74 3.28-8.09z"
        />
        <path
          fill="#34A853"
          d="M12 23c2.97 0 5.46-.98 7.28-2.66l-3.57-2.77c-.98.66-2.23 1.06-3.71 1.06-2.86 0-5.29-1.93-6.16-4.53H2.18v2.84C3.99 20.53 7.7 23 12 23z"
        />
        <path
          fill="#FBBC05"
          d="M5.84 14.09c-.22-.66-.35-1.36-.35-2.09s.13-1.43.35-2.09V7.07H2.18C1.43 8.55 1 10.22 1 12s.43 3.45 1.18 4.93l2.85-2.22.81-.62z"
        />
        <path
          fill="#EA4335"
          d="M12 5.38c1.62 0 3.06.56 4.21 1.64l3.15-3.15C17.45 2.09 14.97 1 12 1 7.7 1 3.99 3.47 2.18 7.07l3.66 2.84c.87-2.6 3.3-4.53 6.16-4.53z"
        />
      </svg>
      Sign in with Google
    </button>
  )
}
```

Create `src/components/auth/sign-out-button.tsx`:

```tsx
"use client"

import { signOut } from "next-auth/react"

export function SignOutButton() {
  return (
    <button
      onClick={() => signOut({ callbackUrl: "/login" })}
      className="rounded-lg border border-gray-300 bg-white px-4 py-2 text-sm text-gray-700 shadow-sm transition-colors hover:bg-gray-50"
    >
      Sign out
    </button>
  )
}
```

Create `src/app/(auth)/login/page.tsx`:

```tsx
import { redirect } from "next/navigation"
import { auth } from "@/lib/auth"
import { SignInButton } from "@/components/auth/sign-in-button"

export default async function LoginPage() {
  const session = await auth()

  // Redirect to dashboard if already logged in
  if (session?.user) {
    redirect("/dashboard")
  }

  return (
    <main className="flex min-h-screen flex-col items-center justify-center bg-gray-50">
      <div className="w-full max-w-md space-y-8 rounded-xl bg-white p-8 shadow-lg">
        <div className="text-center">
          <h1 className="text-3xl font-bold text-gray-900">QuizSwift</h1>
          <p className="mt-2 text-gray-600">
            Sign in to create factually accurate quizzes
          </p>
        </div>

        <div className="flex justify-center pt-4">
          <SignInButton />
        </div>

        <p className="text-center text-xs text-gray-500">
          By signing in, you agree to our Terms of Service and Privacy Policy
        </p>
      </div>
    </main>
  )
}
```

Create `src/app/(dashboard)/layout.tsx`:

```tsx
import { auth } from "@/lib/auth"
import { redirect } from "next/navigation"
import { SignOutButton } from "@/components/auth/sign-out-button"

export default async function DashboardLayout({
  children,
}: {
  children: React.ReactNode
}) {
  const session = await auth()

  if (!session?.user) {
    redirect("/login")
  }

  return (
    <div className="min-h-screen bg-gray-50">
      <header className="border-b bg-white shadow-sm">
        <div className="mx-auto flex max-w-7xl items-center justify-between px-4 py-4">
          <h1 className="text-xl font-semibold text-gray-900">QuizSwift</h1>
          <div className="flex items-center gap-4">
            <span className="text-sm text-gray-600">
              {session.user.name || session.user.email}
            </span>
            <SignOutButton />
          </div>
        </div>
      </header>
      <main className="mx-auto max-w-7xl px-4 py-8">
        {children}
      </main>
    </div>
  )
}
```

Create `src/app/(dashboard)/dashboard/page.tsx`:

```tsx
import { auth } from "@/lib/auth"

export default async function DashboardPage() {
  const session = await auth()

  return (
    <div className="space-y-6">
      <div>
        <h2 className="text-2xl font-bold text-gray-900">Welcome back!</h2>
        <p className="text-gray-600">
          You're signed in as {session?.user?.email}
        </p>
      </div>

      <div className="rounded-lg border bg-white p-6 shadow-sm">
        <h3 className="text-lg font-semibold text-gray-900">Get Started</h3>
        <p className="mt-2 text-gray-600">
          Upload a textbook chapter to create your first quiz. Coming soon in Phase 2!
        </p>
      </div>
    </div>
  )
}
```
  </action>
  <verify>
`npm run build` compiles without errors
Login page renders at /login
Dashboard page renders at /dashboard (when authenticated)
Unauthenticated access to /dashboard redirects to /login
  </verify>
  <done>
Login page with Google sign-in button created. Protected dashboard with user info display. Sign-out functionality works. Route protection via middleware and layout guards.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` completes without TypeScript errors
2. Login page accessible at /login with Google sign-in button
3. Dashboard page shows "Welcome back" with user email
4. Middleware redirects unauthenticated users from /dashboard to /login
5. After sign-in, user is redirected to /dashboard
6. Session persists across browser refresh (close tab, reopen - still logged in)
</verification>

<success_criteria>
- AUTH-01: Teacher can sign in with Google OAuth - Google sign-in button works, OAuth flow completes
- AUTH-04: Sessions persist across browser refresh - database sessions enabled, user remains logged in
- Login page redirects authenticated users to dashboard
- Dashboard displays user name/email from session
- Sign out button clears session and redirects to login
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-compliance/01-02-SUMMARY.md`
</output>
